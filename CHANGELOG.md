# 變更紀錄

本檔每個版本一段。發佈時（推送 `vX.Y.Z` 標籤）GitHub Actions 會自動抽出對應版本的內容，作為該次 Release 的說明。

## v1.0.0

首個正式版本。

### 功能
- 低階鍵鼠鉤子全螢幕鎖定，雙態介面（閒置 / 輸入）。
- 設定主視窗 + 即時預覽（可「重新擷取桌面」），關閉縮到系統匣常駐。
- 全域快捷鍵一鍵鎖定，支援按鍵錄製設定。
- 內建時鐘（字級隨螢幕自動縮放）、背景模糊 / 變暗、高 DPI 與多螢幕支援。
- 主密碼必設 + 可選救援碼（PBKDF2 雜湊，僅存本機）。
- OTP 一次性救援碼：鎖屏按 `F2` 寄送，支援 Telegram / Discord / ntfy.sh，憑證以 Windows DPAPI 加密。
- 自訂藍盾應用程式圖示。

### 安全定位
- 定位為「防隨手亂動（防君子）」，非防內行惡意破解；`Ctrl+Alt+Del` 仍可結束程式。
- 原始碼不含任何機密；所有密碼 / 憑證僅存於各機本地。
