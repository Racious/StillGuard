# 變更紀錄

本檔每個版本一段。發佈時（推送 `vX.Y.Z` 標籤）GitHub Actions 會自動抽出對應版本的內容，作為該次 Release 的說明。

## v1.1.0

駭客終端特效大幅擴充，並新增 Telegram 遠端解鎖。

### 新增
- 偽終端視窗化：macOS 風格外觀（圓角外框、標題列、紅黃綠三燈、置中顯示欄×列）。
- spinner 旋轉等待動畫與三種進度條樣式（經典 / 流動方塊 / 軌道）。
- 大量寫實終端積木：git clone、npm / pip / apt 安裝、docker build、ping、kubectl、dmesg、top 全屏監控等。
- 既有清單 / 服務 / 儀表板 / 叢集 / build 積木加入內部變體與加權抽取，畫面更豐富多變。
- 六種全新形態圖表：sparkline 流量走勢、垂直直方圖、熱力圖、堆疊比例條、tail -f 日誌串流、git log 分支線圖。
- 終端調色盤擴充至 11 色，依語意分類著色（路徑 / 數值 / 標題 / 警告 / 錯誤等）。
- Telegram 遠端解鎖：鎖屏按 `F2` 取得一次性碼後，於手機回覆「/unlock <碼>」即可遠端解鎖（雙重驗證：實體按鍵 + 來源 chat_id + 一次性碼）。
- OTP 寄送與遠端解鎖加入聲音回饋（成功雙高音 / 失敗低音），無畫面情境亦能確認結果。

## v1.0.0

首個正式版本。

### 功能
- 低階鍵鼠鉤子全螢幕鎖定，雙態介面（閒置 / 輸入）。
- 設定主視窗 + 即時預覽（可「重新擷取桌面」），關閉縮到系統匣常駐。
- 全域快捷鍵一鍵鎖定，支援按鍵錄製設定。
- 內建時鐘（字級隨螢幕自動縮放）、背景模糊 / 變暗、高 DPI 與多螢幕支援。
- 主密碼必設 + 可選救援碼（PBKDF2 雜湊，僅存本機）。
- OTP 一次性救援碼：鎖屏按 `F2` 寄送，支援 Telegram / Discord / ntfy.sh，憑證以 Windows DPAPI 加密。
- 自訂藍盾應用程式圖示。

### 安全定位
- 定位為「防隨手亂動（防君子）」，非防內行惡意破解；`Ctrl+Alt+Del` 仍可結束程式。
- 原始碼不含任何機密；所有密碼 / 憑證僅存於各機本地。
