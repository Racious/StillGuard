# 鍵鼠鎖定工具 — 開發設計文件 v1.0

> 一支可常駐桌面、隨手觸發的輕量鎖定程式。觸發後封鎖本機鍵盤與滑鼠，
> 以類 Windows 鎖屏的雙態介面呈現，輸入正確密碼方可解鎖。

---

## 1. 目標與定位

- 提供「暫離座位、防隨手亂動」的快速鎖定，免去每次 `Win+L` 的不便，並可自訂密碼。
- **定位：防隨手亂動（防君子），非防內行惡意破解。**

---

## 2. 安全邊界（重要前提）

| 能擋 | 擋不住 |
|------|--------|
| 一般打字、滑鼠移動與點擊 | **`Ctrl+Alt+Del`** |
| `Win`、`Alt+Tab`、`Win+L`、`Alt+F4` | （按下後進入安全桌面，可開工作管理員結束本程式） |

- 此限制為「無核心驅動（kernel driver）」的先天性質，任何使用者層級工具皆然。
- 若需真正安全，仍應使用系統內建 `Win+L`。

---

## 3. 核心機制

- 語言／框架：**C# + WinForms**，目標 **.NET Framework 4.x**。
- 編譯：使用 Windows 內建 `csc.exe`，產出**單一 `.exe`**，無需安裝 SDK。
- 輸入封鎖：低階鉤子 `WH_KEYBOARD_LL` / `WH_MOUSE_LL`，於 callback 回傳 `1` 吞掉所有鍵鼠事件。
- 顯示：全螢幕、最上層（TopMost）、無邊框視窗，覆蓋整個 `VirtualScreen`（支援多螢幕）；鎖屏期間隱藏游標。

---

## 4. 雙態介面

```
啟動
  └─【閒置態】背景 + 資訊層(Widgets)
        │ 攔截到任意鍵 / 滑鼠事件 → 觸發切換
        ▼
     【輸入態】浮出密碼面板（● 遮罩、錯誤提示）
        │ 密碼正確
        ▼
     解鎖、關閉程式、釋放鉤子
        ‖ 輸入態無操作逾時（預設 10 秒）→ 退回【閒置態】
```

- 鍵鼠**全程被鉤子封鎖**。
- 「喚醒」是攔截到輸入後拿來切換 UI 狀態，**實體輸入不會傳入系統**。

---

## 5. 背景層（可由設定檔切換）

| 類型 | 說明 |
|------|------|
| `blurDesktop`（**預設**） | 啟動瞬間擷取桌面 → 縮小 → 高斯模糊 → 放大繪製 + 輕微變暗。保留畫面感，又因糊化而防偷看。 |
| `image` | 顯示自選圖片，可選擇是否加模糊／暗角。 |
| `solidDark` | 純深色，最省資源。 |

### 模糊實作備註
- 採「截圖 → 縮小 → 模糊 → 放大」策略：先將截圖縮至約 1/8 再做模糊運算，再以雙線性插值放大繪製，兼顧效能與平滑度。
- 跨 Windows 版本一致、效果可控；優於系統 Acrylic 即時模糊（後者列為備選）。

---

## 6. 資訊層 — 可擴充 Widget 架構 ⭐

設定檔 `config.json` 列出要顯示的元件，程式以工廠／策略模式逐一渲染。
新增元件 = 新增一個 renderer + 於工廠註冊，符合開放封閉原則。

### 設定範例

```json
{
  "background": { "type": "blurDesktop", "blur": 18, "dim": 0.25 },
  "idleTimeoutSec": 10,
  "widgets": [
    { "type": "clock",   "format": "HH:mm",            "y": "30%", "fontSize": 72 },
    { "type": "date",    "format": "yyyy/MM/dd dddd",  "y": "42%", "fontSize": 24 },
    { "type": "text",    "content": "老爺暫離 · 請勿觸碰", "y": "52%", "fontSize": 20 },
    { "type": "weather", "city": "Taipei",             "y": "60%", "fontSize": 20, "enabled": false },
    { "type": "hint",    "content": "按任意鍵或點擊以解鎖", "y": "90%", "fontSize": 16 }
  ]
}
```

### 元件清單

| 元件 | 狀態 | 說明 |
|------|------|------|
| `clock` | 內建 | 時鐘，`format` 控制格式 |
| `date` | 內建 | 日期星期 |
| `text` | 內建 | 自訂靜態文字 |
| `hint` | 內建 | 解鎖引導文字 |
| `weather` | 預留（預設關閉） | 以免金鑰服務 wttr.in 背景抓取。**註：會送出城市名至外部服務**，故預設 `enabled:false`，由老爺自行開啟。 |

### 架構示意

```
IWidget                 // 介面：Render(Graphics g, Rectangle screen)
 ├─ ClockWidget
 ├─ DateWidget
 ├─ TextWidget
 ├─ HintWidget
 └─ WeatherWidget       // 未來擴充

WidgetFactory.Create(type, config) → IWidget
```

---

## 7. 密碼策略

- **主密碼** + **緊急備用碼**，兩組擇一皆可解鎖。
- **寫死於程式碼**（不放入設定檔），避免明碼外洩；變更密碼需重新編譯。
- 輸入處理：鉤子內以 `ToUnicode` 組字串。
  - `Enter`：比對密碼。
  - `Backspace`：刪除一字。
  - `Esc`：清空輸入。
  - 錯誤：清空並顯示「密碼錯誤」。

---

## 8. 設定總覽（`config.json`，免重編譯可改）

- 背景類型、模糊強度、變暗程度。
- 閒置逾時秒數。
- 資訊層元件清單與各自樣式（位置、字級、格式、內容）。
- **唯密碼例外——寫死於程式碼。**

---

## 9. 風險與備案

| 風險 | 對策 |
|------|------|
| 程式當掉導致鍵鼠卡死 | 鉤子由本程式持有，程式一結束即自動釋放；最壞情況 `Ctrl+Alt+Del` → 工作管理員結束 |
| 忘記主密碼 | 緊急備用碼為後路；皆忘則 `Ctrl+Alt+Del` 解圍 |
| 防毒誤判低階鉤子 | 自行編譯之本機 exe，必要時加防毒白名單 |
| 天氣元件外洩城市名 | 預設關閉，由老爺自行啟用 |

---

## 10. 交付物

- `DESIGN.md`（本文件）
- `LockScreen.cs`（原始碼，含 Widget 架構）
- `config.json`（範例設定）
- `build.bat`（一鍵編譯）
- `README.md`（使用說明）

---

## 11. 待辦／未來擴充

- [ ] 全域熱鍵常駐（例如 `Ctrl+Alt+L` 一鍵鎖），免雙擊執行。
- [ ] 逾時自動觸發系統 `Win+L` 真鎖（更高安全性選項）。
- [ ] 連錯 N 次延長鎖定或真鎖機。
- [ ] `weather` 元件正式實作與更多資訊元件（行事曆、系統狀態等）。
