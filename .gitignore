# ── 個人設定（含密碼雜湊 / 未來的通道憑證）——絕不上傳 ──
config.json

# ── 內部設計文件（本地保留，不公開上傳）──
DESIGN.md

# ── 編譯產物 ──
*.exe
*.pdb
*.obj

# ── 發佈流程暫存產物（由 GitHub Actions 產生）──
release-body.md
dist/
StillGuard-*.zip

# ── 編輯器 / 系統 ──
.vs/
.idea/
Thumbs.db
desktop.ini
*~
